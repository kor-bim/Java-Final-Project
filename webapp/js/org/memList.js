function isEmptyObject(param) {
	return Object.keys(param).length === 0 && param.constructor === Object;
}


var setting_for_ztree = {
		data: {
	        simpleData: {
	            enable: true
	        }
	    },
	    view : {
	    	showIcon : false,
	    	fontCss : {color:"red"}
	    }
	}
